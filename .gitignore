# Dependencies
node_modules/

# Build / runtime outputs
# NOTE: client/dist IS committed on purpose so `npm start` serves the React
# front-end at / without requiring a build step. Rebuild with: cd client && npm run build
dist-ssr/
*.local

# Backend (Node.js + Express + SQLite)
server/node_modules/
server/data/

# React front-end (client/)
client/node_modules/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS junk
.DS_Store
Thumbs.db
Desktop.ini